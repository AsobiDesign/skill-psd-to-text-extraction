---
name: psd-to-text-extraction
description: PSD（Photoshopデザインカンプ）からテキストだけを抜き出す。原稿の書き起こし、文言の一覧化、コピーの校正・チェック、翻訳や外注に渡すテキスト作成、CMSやECへの文言流し込み、文言差分の確認といった場面で必ず使う。「PSDの文字だけ欲しい」「テキストを抜いて」「原稿に起こして」「文言を一覧にして」と言われたら迷わずこれを使うこと。出力は羅列版（.txt）とHTML整形版（.html）の2種類で、表組みは自動で<table>になる。なおHTML/CSSを実装してページを作るのが目的なら、このスキルではなく psd-to-html を使う。
---

# PSDからのテキスト抽出

デザインカンプの文字は画像ではなくテキストレイヤーとして入っている。
目で読み取って打ち直す必要はなく、実文字列をそのまま取り出せる。
誤字の混入もコピペ漏れも起きない。

このスキルは**文言だけ**を扱う。ページを実装するなら `psd-to-html` を使う。

## 依存

`psd-tools>=1.17` だけ。ImageMagickもPhotoshopも要らないので、WindowsでもmacOSでも同じ手順で動く。

```bash
pip install "psd-tools>=1.17"
```

1.10系は新しいPhotoshopが書いたPSDを読めずエラーになる（`Invalid version 8`）。
`AssertionError` が出たらまずバージョンを疑う。

## 使い方

```bash
# 2種類まとめて書き出す（通常はこれ）
python scripts/psd_text.py design.psd -o out/

# 画面を絞る／標準出力で確認する
python scripts/psd_text.py design.psd --screen SP_top --txt
```

`out/` には次の2つができる。

| ファイル | 中身 | 向いている用途 |
|---|---|---|
| `<名前>-text.txt` | 読み順に文字列を並べただけ | 原稿として渡す、校正、翻訳依頼、文言のdiff |
| `<名前>-text.html` | 見出し・リスト・表に整えたもの | 構造を確認する、CMSへの下書き、クライアント共有 |

引数なしで実行すると、画面ごとの件数だけが出る。まず全体量を掴みたいときに使う。

## 出力の中身

### 羅列版

画面 → セクション（PSDの最上位グループ名）→ 読み順、で並ぶ。
装飾情報は付かないので、そのままテキスト原稿として使える。

```
============================================================
PC_top  (pc / 1920x4242)
============================================================

--- news ---
news
2022.12.20
サービス
【プレスリリース】新サービス「サンプルサービス」をリリース！
```

### HTML整形版

座標と文字サイズから役割を推定してタグを当てる。

- **表** — 2列以上の行が2行以上続き、列の左端が揃っていれば `<table>`。
  カンプに罫線が引かれていなくても、座標が揃っていれば人は表として読むので、
  座標だけで判断する。お知らせ一覧や料金表がここに入る。
- **見出し** — その画面で最も多く使われている文字サイズを本文とみなし、
  それより大きいものを `h2`〜`h4` に割り当てる。絶対値で決めないのは、
  PCとSPで同じ見出しでもサイズが違うため。
- **リスト** — 短い文字列が同じ左端で等間隔に縦に並んでいれば `<ul>`。
- それ以外は `<p>`。太字の短い行は `<strong>` を付ける。

## 非表示レイヤーも拾う

デザイナーは「開いた状態のハンバーガーメニュー」「タブの裏側」「ホバー時の表示」を
非表示レイヤーとして同じPSDに置いておく。**そこにある文言もページには載る**ので、
原稿としては必要になる。

このスクリプトは非表示グループも読む（PSDファイル自体は変更しない）。ただし
通常表示のものとは混ぜず、別の塊として出す。

- 羅列版 → `［別状態（PSDで非表示）］` として字下げして続ける
- HTML版 → 黄色い枠の「別状態」ブロックにまとめる

混ぜないのには理由がある。タブの裏側は表示中の要素と**同じ座標に重なっている**ため、
一緒に並べると行や列の判定が壊れて、表がぐちゃぐちゃになる。

## アートボードの有無

アートボードで作られたPSDと、1枚のキャンバスに直接組まれたPSDのどちらも扱える。

- アートボードがある → それぞれを1画面として扱う（`detect: artboard`）
- 無いが最上位グループ名がPC/SPを示している → 横並びの2カンプとみなす（`detect: group`）
- どちらでもない → キャンバス全体を1画面とする（`detect: canvas`）

`detect: canvas` と出たときは画面の切り分けが推測になっている。
セクション分けが妥当か、出力を一度目で確かめる。

## つまずきやすいところ

- **同じ文言が2つ出てきたら、ホバー用の重なり。** 同じ座標に色違いで重ねる作りが
  普通なので、スクリプト側で近い位置の同一文言はまとめている。それでも残る場合は、
  座標が大きく離れた別物（実際に2箇所に書かれている）と考えてよい。
- **改行はPSDの改行がそのまま入る。** カンプの見た目を優先した改行（「サンプル飲料／
  公式Instagram運用」など）が含まれるので、原稿として渡す前に意図した改行かを確認する。
- **文字が画像化されている箇所は取れない。** ロゴやバナーの中の文字はテキスト
  レイヤーではないため出てこない。件数が明らかに少ないときはこれを疑う。
